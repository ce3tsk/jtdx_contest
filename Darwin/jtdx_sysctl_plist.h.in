// CE3TSK: generated by CMake from Darwin/com.jtdx.sysctl.plist - edit that file, not this one.
// The launch daemon's text, compiled into the program so that what main.cpp installs as root is
// not read from the app bundle at run time (see mac_shared_memory.hpp).
#ifndef JTDX_SYSCTL_PLIST_H
#define JTDX_SYSCTL_PLIST_H
static char const jtdx_sysctl_plist[] = R"JTDX_PLIST(@JTDX_SYSCTL_PLIST@)JTDX_PLIST";
#endif
